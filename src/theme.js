import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#FEF5AC",
    },
    secondary: {
      main: "#000",
    },
    background: {
      paper: "#25316D",
      default: "#25316D",
    },
  },
});
